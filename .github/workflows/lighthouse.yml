name: Lighthouse CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  lighthouse:
    name: Lighthouse Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"

      - run: npm ci

      - name: Build
        run: npm run build
        env:
          ANTHROPIC_API_KEY: build-placeholder
          STRIPE_SECRET_KEY: build-placeholder
          COOKIE_SECRET: build-placeholder
          NEXT_PUBLIC_POSTHOG_KEY: build-placeholder

      - name: Start server
        run: npm run start &
        env:
          ANTHROPIC_API_KEY: test-placeholder
          STRIPE_SECRET_KEY: test-placeholder
          COOKIE_SECRET: test-placeholder
          NEXT_PUBLIC_POSTHOG_KEY: test-placeholder

      - name: Wait for server
        run: npx wait-on http://localhost:3000 --timeout 30000

      - name: Run Lighthouse
        uses: treosh/lighthouse-ci-action@v12
        with:
          urls: |
            http://localhost:3000
            http://localhost:3000/tools
            http://localhost:3000/pricing
            http://localhost:3000/about
            http://localhost:3000/faq
          budgetPath: .github/lighthouse/budget.json
          uploadArtifacts: true
          temporaryPublicStorage: true
