Quick project status — keep it concise:

1. Git branch + uncommitted changes (`git status`)
2. Last 3 commits (`git log --oneline -3`)
3. Open beads issues (`bd list --json`)
4. Ready tasks (`bd ready --json`)
5. Blocked tasks
6. Summary of `memory/HANDOFF.md` if it exists
